### YamlMime:UniversalReference
items:
  - uid: office.Office.Document
    summary: '<span data-ttu-id="c9f63-101">Eine abstrakte Klasse, die das Dokument darstellt, mit dem das Add-In interagiert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-101">An abstract class that represents the document the add-in is interacting with.</span></span>'
    remarks: '<span data-ttu-id="c9f63-102">**Hosts**: Excel, PowerPoint, Project, Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-102">**Hosts**: Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.Document.addHandlerAsync
      - office.Office.Document.addHandlerAsync_1
      - office.Office.Document.getActiveViewAsync
      - office.Office.Document.getActiveViewAsync_1
      - office.Office.Document.getFileAsync
      - office.Office.Document.getFileAsync_1
      - office.Office.Document.getFilePropertiesAsync
      - office.Office.Document.getFilePropertiesAsync_1
      - office.Office.Document.getMaxResourceIndexAsync
      - office.Office.Document.getMaxResourceIndexAsync_1
      - office.Office.Document.getMaxTaskIndexAsync
      - office.Office.Document.getMaxTaskIndexAsync_1
      - office.Office.Document.getProjectFieldAsync
      - office.Office.Document.getProjectFieldAsync_1
      - office.Office.Document.getResourceByIndexAsync
      - office.Office.Document.getResourceByIndexAsync_1
      - office.Office.Document.getResourceFieldAsync
      - office.Office.Document.getResourceFieldAsync_1
      - office.Office.Document.getSelectedDataAsync
      - office.Office.Document.getSelectedDataAsync_1
      - office.Office.Document.getSelectedResourceAsync
      - office.Office.Document.getSelectedResourceAsync_1
      - office.Office.Document.getSelectedTaskAsync
      - office.Office.Document.getSelectedTaskAsync_1
      - office.Office.Document.getSelectedViewAsync
      - office.Office.Document.getSelectedViewAsync_1
      - office.Office.Document.getTaskAsync
      - office.Office.Document.getTaskAsync_1
      - office.Office.Document.getTaskByIndexAsync
      - office.Office.Document.getTaskByIndexAsync_1
      - office.Office.Document.getTaskFieldAsync
      - office.Office.Document.getTaskFieldAsync_1
      - office.Office.Document.getWSSUrlAsync
      - office.Office.Document.getWSSUrlAsync_1
      - office.Office.Document.goToByIdAsync
      - office.Office.Document.goToByIdAsync_1
      - office.Office.Document.removeHandlerAsync
      - office.Office.Document.removeHandlerAsync_1
      - office.Office.Document.setResourceFieldAsync
      - office.Office.Document.setResourceFieldAsync_1
      - office.Office.Document.setSelectedDataAsync
      - office.Office.Document.setSelectedDataAsync_1
      - office.Office.Document.setTaskFieldAsync
      - office.Office.Document.setTaskFieldAsync_1
      - office.Office.Document.bindings
      - office.Office.Document.customXmlParts
      - office.Office.Document.mode
      - office.Office.Document.settings
      - office.Office.Document.url
  - uid: office.Office.Document.addHandlerAsync
    summary: <span data-ttu-id="c9f63-103">Fügt einen Ereignishandler für ein Document-Objekt Ereignis hinzu.</span><span class="sxs-lookup"><span data-stu-id="c9f63-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-104">**Anforderungsgruppe**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="c9f63-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="c9f63-105">Sie können mehrere Ereignishandler für den angegebenen EventType hinzufügen, solange der Name jeder Ereignis Handlerfunktion eindeutig ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="c9f63-106">Für ein Document-Objekt Ereignis kann der eventType-Parameter als `Office.EventType.Document.SelectionChanged` oder angegeben werden.`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="c9f63-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="c9f63-107">oder den entsprechenden Textwert dieser Aufzählung.</span><span class="sxs-lookup"><span data-stu-id="c9f63-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - office.Office.EventType
        - id: handler
          description: '<span data-ttu-id="c9f63-108">Die hinzuzufügende Ereignis Handlerfunktion, deren einziger Parameter vom Typ [Office. DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="c9f63-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="c9f63-109">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-109"></span></span> <span data-ttu-id="c9f63-110">Erforderlich.</span><span class="sxs-lookup"><span data-stu-id="c9f63-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="c9f63-111">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-112">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-112">Optional.</span></span> <span data-ttu-id="c9f63-113">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-114">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-114"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.addHandlerAsync_1
    summary: <span data-ttu-id="c9f63-115">Fügt einen Ereignishandler für ein Document-Objekt Ereignis hinzu.</span><span class="sxs-lookup"><span data-stu-id="c9f63-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-116">**Anforderungsgruppe**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="c9f63-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="c9f63-117">Sie können mehrere Ereignishandler für den angegebenen EventType hinzufügen, solange der Name jeder Ereignis Handlerfunktion eindeutig ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-118">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="c9f63-119">Für ein Document-Objekt Ereignis kann der eventType-Parameter als `Office.EventType.Document.SelectionChanged` oder angegeben werden.`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="c9f63-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="c9f63-120">oder den entsprechenden Textwert dieser Aufzählung.</span><span class="sxs-lookup"><span data-stu-id="c9f63-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - office.Office.EventType
        - id: handler
          description: '<span data-ttu-id="c9f63-121">Die hinzuzufügende Ereignis Handlerfunktion, deren einziger Parameter vom Typ [Office. DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="c9f63-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="c9f63-122">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-122"></span></span> <span data-ttu-id="c9f63-123">Erforderlich.</span><span class="sxs-lookup"><span data-stu-id="c9f63-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="c9f63-124">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-124">Optional.</span></span> <span data-ttu-id="c9f63-125">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-126">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-126"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.getActiveViewAsync
    summary: <span data-ttu-id="c9f63-127">Gibt den Status der aktuellen Ansicht der Präsentation ("Bearbeiten" oder "Lesen") zurück.</span><span class="sxs-lookup"><span data-stu-id="c9f63-127">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-128">**Anforderungsgruppe**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="c9f63-128">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="c9f63-129">Kann ein Ereignis auslösen, wenn die Ansicht geändert wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-129">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-130">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-130">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-131">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-131">Optional.</span></span> <span data-ttu-id="c9f63-132">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-132">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-133">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-133"></span></span> <span data-ttu-id="c9f63-134">Die `value` Eigenschaft des Ergebnisses entspricht dem Status der aktuellen Ansicht der Präsentation.</span><span class="sxs-lookup"><span data-stu-id="c9f63-134">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="c9f63-135">Der zurückgegebene Wert kann entweder "Edit" oder "Read" lauten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-135">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="c9f63-136">"Bearbeiten" entspricht einer der Ansichten, in denen Sie Folien bearbeiten können, beispielsweise "Normal" oder "Gliederungsansicht".</span><span class="sxs-lookup"><span data-stu-id="c9f63-136">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="c9f63-137">"Read" entspricht entweder der Bildschirmpräsentation oder der Leseansicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-137">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getActiveViewAsync_1
    summary: <span data-ttu-id="c9f63-138">Gibt den Status der aktuellen Ansicht der Präsentation ("Bearbeiten" oder "Lesen") zurück.</span><span class="sxs-lookup"><span data-stu-id="c9f63-138">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-139">**Anforderungsgruppe**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="c9f63-139">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="c9f63-140">Kann ein Ereignis auslösen, wenn die Ansicht geändert wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-140">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-141">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-141">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-142">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-142">Optional.</span></span> <span data-ttu-id="c9f63-143">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-143">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-144">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-144"></span></span> <span data-ttu-id="c9f63-145">Die `value` Eigenschaft des Ergebnisses entspricht dem Status der aktuellen Ansicht der Präsentation.</span><span class="sxs-lookup"><span data-stu-id="c9f63-145">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="c9f63-146">Der zurückgegebene Wert kann entweder "Edit" oder "Read" lauten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-146">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="c9f63-147">"Bearbeiten" entspricht einer der Ansichten, in denen Sie Folien bearbeiten können, beispielsweise "Normal" oder "Gliederungsansicht".</span><span class="sxs-lookup"><span data-stu-id="c9f63-147">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="c9f63-148">"Read" entspricht entweder der Bildschirmpräsentation oder der Leseansicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-148">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getFileAsync
    summary: "<span data-ttu-id=\"c9f63-149\">Gibt die gesamte Dokumentdatei in Segmenten von bis zu 4194304\_Byte (4\_MB) zurück.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-149\">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"c9f63-150\">Für Add-Ins auf IOS wird der Dateiausschnitt bis zu 65536 (64 KB) unterstützt.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-150\">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id=\"c9f63-151\">Beachten Sie, dass Angabe einer Foliendateigröße oberhalb des zulässigen Grenzwerts zu einem \"Internen Fehler\" führt.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-151\">Note that specifying file slice size of above permitted limit will result in an \"Internal Error\" failure.</span></span>"
    remarks: |-
      <span data-ttu-id="c9f63-152">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-152">**Requirement sets**:</span></span>

      - <span data-ttu-id="c9f63-153">[](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) Komprimierte (bei Verwendung von`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-153">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-154">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-154"></span></span>

      - [<span data-ttu-id="c9f63-155">Datei</span><span class="sxs-lookup"><span data-stu-id="c9f63-155">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="c9f63-156">[Textfile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (bei Verwendung von`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-156">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="c9f63-157">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-157"></span></span>

      <span data-ttu-id="c9f63-158">Bei Add-Ins, die in Office-Hostanwendungen mit Ausnahme von Office auf IOS durchführen, unterstützt die getFileAsync-Methode das erhalten von Dateien in Slices mit bis zu 4194304 Byte (4 MB).</span><span class="sxs-lookup"><span data-stu-id="c9f63-158">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="c9f63-159">Bei Add-Ins, die in Office-Apps unter IOS durchführen, unterstützt die getFileAsync-Methode das erhalten von Dateien in Slices von bis zu 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="c9f63-159">For add-ins running in Office apps on iOS, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="c9f63-160">Der fileType-Parameter kann mithilfe der [Office. filetype](xref:office.Office.FileType) -Aufzählung oder von Textwerten angegeben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-160">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="c9f63-161">Die möglichen Werte variieren jedoch mit dem Host:</span><span class="sxs-lookup"><span data-stu-id="c9f63-161">But the possible values vary with the host:</span></span>

      <span data-ttu-id="c9f63-162">Excel im Internet und Windows-Desktop:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-162">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="c9f63-163">Excel auf Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-163">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-164">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-164"></span></span>

      <span data-ttu-id="c9f63-165">PowerPoint im Internet, Windows-Desktop, Mac und iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-165">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-166">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-166"></span></span>

      <span data-ttu-id="c9f63-167">Word im Internet, Windows-Desktop, Mac und iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-167">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-168">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-168"></span></span><!-- --><span data-ttu-id="c9f63-169">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-169"></span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-170">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-170">Examples</span></span>

      ```javascript
      // The following example gets the document in Office Open XML ("compressed") format in 65536 bytes (64 KB) slices.
      // Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.
      function getDocumentAsCompressed() {
          Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, 
              function (result) {
                  if (result.status == "succeeded") {
                  // If the getFileAsync call succeeded, then
                  // result.value will return a valid File Object.
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);

                  // Get the file slices.
                  getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
                  else {
                  app.showNotification("Error:", result.error.message);
                  }
          });
      }

      function getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {
          file.getSliceAsync(nextSlice, function (sliceResult) {
              if (sliceResult.status == "succeeded") {
                  if (!gotAllSlices) { // Failed to get all slices, no need to continue.
                      return;
                  }

                  // Got one slice, store it in a temporary array.
                  // (Or you can do something else, such as
                  // send it to a third-party server.)
                  docdataSlices[sliceResult.value.index] = sliceResult.value.data;
                  if (++slicesReceived == sliceCount) {
                    // All slices have been received.
                    file.closeAsync();
                    onGotAllSlices(docdataSlices);
                  }
                  else {
                      getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
              }
                  else {
                      gotAllSlices = false;
                      file.closeAsync();
                      app.showNotification("getSliceAsync Error:", sliceResult.error.message);
                  }
          });
      }

      function onGotAllSlices(docdataSlices) {
          var docdata = [];
          for (var i = 0; i < docdataSlices.length; i++) {
              docdata = docdata.concat(docdataSlices[i]);
          }

          var fileContent = new String();
          for (var j = 0; j < docdata.length; j++) {
              fileContent += String.fromCharCode(docdata[j]);
          }

          // Now all the file content is stored in 'fileContent' variable,
          // you can do something with it, such as print, fax...
      }

      // The following example gets the document in PDF format.
      Office.context.document.getFileAsync(Office.FileType.Pdf,
          function(result) {
              if (result.status == "succeeded") {
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);
                  // Now, you can call getSliceAsync to download the files,
                  // as described in the previous code segment (compressed format).
                  
                  myFile.closeAsync();
              }
              else {
                  app.showNotification("Error:", result.error.message);
              }
      }
      );
      ```
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: '<span data-ttu-id="c9f63-171">Das Format, in dem die Datei zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-171">The format in which the file will be returned</span></span>'
          type:
            - office.Office.FileType
        - id: options
          description: '<span data-ttu-id="c9f63-172">Enthält Optionen zum Festlegen der Größe von Segmenten, in die das Dokument aufgeteilt wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-172">Provides options for setting the size of slices that the document will be divided into.</span></span>'
          type:
            - office.Office.GetFileOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-173">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-173">Optional.</span></span> <span data-ttu-id="c9f63-174">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-174">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-175">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-175"></span></span> <span data-ttu-id="c9f63-176">Die `value` Eigenschaft des Ergebnisses ist das File-Objekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-176">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFileAsync_1
    summary: "<span data-ttu-id=\"c9f63-177\">Gibt die gesamte Dokumentdatei in Segmenten von bis zu 4194304\_Byte (4\_MB) zurück.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-177\">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id=\"c9f63-178\">Für Add-Ins auf IOS wird der Dateiausschnitt bis zu 65536 (64 KB) unterstützt.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-178\">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id=\"c9f63-179\">Beachten Sie, dass Angabe einer Foliendateigröße oberhalb des zulässigen Grenzwerts zu einem \"Internen Fehler\" führt.</span><span class=\"sxs-lookup\"><span data-stu-id=\"c9f63-179\">Note that specifying file slice size of above permitted limit will result in an \"Internal Error\" failure.</span></span>"
    remarks: |-
      <span data-ttu-id="c9f63-180">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-180">**Requirement sets**:</span></span>

      - <span data-ttu-id="c9f63-181">[](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) Komprimierte (bei Verwendung von`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-181">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-182">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-182"></span></span>

      - [<span data-ttu-id="c9f63-183">Datei</span><span class="sxs-lookup"><span data-stu-id="c9f63-183">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="c9f63-184">[Textfile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (bei Verwendung von`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-184">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="c9f63-185">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-185"></span></span>

      <span data-ttu-id="c9f63-186">Bei Add-Ins, die in Office-Hostanwendungen mit Ausnahme von Office auf IOS durchführen, unterstützt die getFileAsync-Methode das erhalten von Dateien in Slices mit bis zu 4194304 Byte (4 MB).</span><span class="sxs-lookup"><span data-stu-id="c9f63-186">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="c9f63-187">Bei Add-Ins, die in Office in ios-apps durchführen, unterstützt die getFileAsync-Methode das erhalten von Dateien in Slices von bis zu 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="c9f63-187">For add-ins running in Office on iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="c9f63-188">Der fileType-Parameter kann mithilfe der [Office. filetype](xref:office.Office.FileType) -Aufzählung oder von Textwerten angegeben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-188">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="c9f63-189">Die möglichen Werte variieren jedoch mit dem Host:</span><span class="sxs-lookup"><span data-stu-id="c9f63-189">But the possible values vary with the host:</span></span>

      <span data-ttu-id="c9f63-190">Excel im Internet und Windows-Desktop:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-190">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="c9f63-191">Excel auf Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-191">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-192">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-192"></span></span>

      <span data-ttu-id="c9f63-193">PowerPoint im Internet, Windows-Desktop, Mac und iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-193">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-194">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-194"></span></span>

      <span data-ttu-id="c9f63-195">Word im Internet, Windows-Desktop, Mac und iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="c9f63-195">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="c9f63-196">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="c9f63-196"></span></span><!-- --><span data-ttu-id="c9f63-197">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-197"></span></span>
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: '<span data-ttu-id="c9f63-198">Das Format, in dem die Datei zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-198">The format in which the file will be returned</span></span>'
          type:
            - office.Office.FileType
        - id: callback
          description: '<span data-ttu-id="c9f63-199">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-199">Optional.</span></span> <span data-ttu-id="c9f63-200">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-200">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-201">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-201"></span></span> <span data-ttu-id="c9f63-202">Die `value` Eigenschaft des Ergebnisses ist das File-Objekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-202">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync
    summary: <span data-ttu-id="c9f63-203">Ruft Dateieigenschaften des aktuellen Dokuments ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-203">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-204">**Anforderungs Sätze**: [nicht in einer Gruppe](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="c9f63-204">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="c9f63-205">Die URL der Datei wird mit der URL-Eigenschaft abgerufen.`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="c9f63-205">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="c9f63-206">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-206"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-207">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-207">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-208">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-208">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-209">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-209"></span></span> <span data-ttu-id="c9f63-210">Die `value` Eigenschaft des Ergebnisses sind die Eigenschaften der Datei (mit der URL unter`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="c9f63-210">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="c9f63-211">).</span><span class="sxs-lookup"><span data-stu-id="c9f63-211"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync_1
    summary: <span data-ttu-id="c9f63-212">Ruft Dateieigenschaften des aktuellen Dokuments ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-212">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-213">**Anforderungs Sätze**: [nicht in einer Gruppe](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="c9f63-213">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="c9f63-214">Die URL der Datei wird mit der URL-Eigenschaft abgerufen.`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="c9f63-214">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="c9f63-215">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-215"></span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-216">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-216">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-217">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-217">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-218">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-218"></span></span> <span data-ttu-id="c9f63-219">Die `value` Eigenschaft des Ergebnisses sind die Eigenschaften der Datei (mit der URL unter`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="c9f63-219">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="c9f63-220">).</span><span class="sxs-lookup"><span data-stu-id="c9f63-220"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync
    summary: |-
      <span data-ttu-id="c9f63-221">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-221">Project documents only.</span></span> <span data-ttu-id="c9f63-222">Rufen Sie den maximalen Index der Sammlung von Ressourcen im aktuellen Projekt ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-222">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="c9f63-223">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-223">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-224">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-224">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-225">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-225">Optional.</span></span> <span data-ttu-id="c9f63-226">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-226">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-227">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-227"></span></span> <span data-ttu-id="c9f63-228">Die `value` Eigenschaft des Ergebnisses ist die höchste Indexnummer in der Ressourcensammlung des aktuellen Projekts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-228">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync_1
    summary: |-
      <span data-ttu-id="c9f63-229">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-229">Project documents only.</span></span> <span data-ttu-id="c9f63-230">Rufen Sie den maximalen Index der Sammlung von Ressourcen im aktuellen Projekt ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-230">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="c9f63-231">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-231">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-232">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-232">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-233">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-233">Optional.</span></span> <span data-ttu-id="c9f63-234">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-234">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-235">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-235"></span></span> <span data-ttu-id="c9f63-236">Die `value` Eigenschaft des Ergebnisses ist die höchste Indexnummer in der Ressourcensammlung des aktuellen Projekts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-236">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync
    summary: |-
      <span data-ttu-id="c9f63-237">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-237">Project documents only.</span></span> <span data-ttu-id="c9f63-238">Rufen Sie den maximalen Index der Auflistung von Aufgaben im aktuellen Projekt ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-238">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="c9f63-239">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-239">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-240">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-240">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-241">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-241">Optional.</span></span> <span data-ttu-id="c9f63-242">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-242">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-243">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-243"></span></span> <span data-ttu-id="c9f63-244">Die `value` Eigenschaft des Ergebnisses ist die höchste Indexnummer in der Aufgabensammlung des aktuellen Projekts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-244">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync_1
    summary: |-
      <span data-ttu-id="c9f63-245">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-245">Project documents only.</span></span> <span data-ttu-id="c9f63-246">Rufen Sie den maximalen Index der Auflistung von Aufgaben im aktuellen Projekt ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-246">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="c9f63-247">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-247">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-248">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-248">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-249">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-249">Optional.</span></span> <span data-ttu-id="c9f63-250">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-250">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-251">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-251"></span></span> <span data-ttu-id="c9f63-252">Die `value` Eigenschaft des Ergebnisses ist die höchste Indexnummer in der Aufgabensammlung des aktuellen Projekts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-252">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getProjectFieldAsync
    summary: <span data-ttu-id="c9f63-253">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-253">Project documents only.</span></span> <span data-ttu-id="c9f63-254">Projektfeld abrufen (ex.</span><span class="sxs-lookup"><span data-stu-id="c9f63-254">Get Project field (Ex.</span></span> <span data-ttu-id="c9f63-255">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="c9f63-255">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="c9f63-256">Felder auf Projektebene</span><span class="sxs-lookup"><span data-stu-id="c9f63-256">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="c9f63-257">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-257">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-258">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-258">Optional.</span></span> <span data-ttu-id="c9f63-259">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-259">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-260">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-260"></span></span> <span data-ttu-id="c9f63-261">Die `value` Eigenschaft des Ergebnisses enthält die `fieldValue` -Eigenschaft, die den Wert des angegebenen Felds darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-261">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getProjectFieldAsync_1
    summary: <span data-ttu-id="c9f63-262">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-262">Project documents only.</span></span> <span data-ttu-id="c9f63-263">Projektfeld abrufen (ex.</span><span class="sxs-lookup"><span data-stu-id="c9f63-263">Get Project field (Ex.</span></span> <span data-ttu-id="c9f63-264">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="c9f63-264">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-265">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-265">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="c9f63-266">Felder auf Projektebene</span><span class="sxs-lookup"><span data-stu-id="c9f63-266">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="c9f63-267">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-267">Optional.</span></span> <span data-ttu-id="c9f63-268">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-268">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-269">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-269"></span></span> <span data-ttu-id="c9f63-270">Die `value` Eigenschaft des Ergebnisses enthält die `fieldValue` -Eigenschaft, die den Wert des angegebenen Felds darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-270">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync
    summary: |-
      <span data-ttu-id="c9f63-271">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-271">Project documents only.</span></span> <span data-ttu-id="c9f63-272">Rufen Sie die GUID der Ressource ab, die den angegebenen Index in der Ressourcensammlung aufweist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-272">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="c9f63-273">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-273">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="c9f63-274">Der Index der Ressource in der Sammlung von Ressourcen für das Projekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-274">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="c9f63-275">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-275">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-276">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-276">Optional.</span></span> <span data-ttu-id="c9f63-277">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-277">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-278">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-278"></span></span> <span data-ttu-id="c9f63-279">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-279">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync_1
    summary: |-
      <span data-ttu-id="c9f63-280">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-280">Project documents only.</span></span> <span data-ttu-id="c9f63-281">Rufen Sie die GUID der Ressource ab, die den angegebenen Index in der Ressourcensammlung aufweist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-281">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="c9f63-282">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-282">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-283">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-283">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="c9f63-284">Der Index der Ressource in der Sammlung von Ressourcen für das Projekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-284">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="c9f63-285">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-285">Optional.</span></span> <span data-ttu-id="c9f63-286">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-286">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-287">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-287"></span></span> <span data-ttu-id="c9f63-288">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-288">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync
    summary: <span data-ttu-id="c9f63-289">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-289">Project documents only.</span></span> <span data-ttu-id="c9f63-290">Ressourcenfeld für die angegebene Ressourcen-ID abrufen. (ex. resourceName)</span><span class="sxs-lookup"><span data-stu-id="c9f63-290">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="c9f63-291">Entweder eine Zeichenfolge oder ein Wert der Ressourcen-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-291">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-292">Ressourcenfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-292">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="c9f63-293">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-293">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-294">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-294">Optional.</span></span> <span data-ttu-id="c9f63-295">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-295">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-296">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-296"></span></span> <span data-ttu-id="c9f63-297">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-297">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync_1
    summary: <span data-ttu-id="c9f63-298">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-298">Project documents only.</span></span> <span data-ttu-id="c9f63-299">Ressourcenfeld für die angegebene Ressourcen-ID abrufen. (ex. resourceName)</span><span class="sxs-lookup"><span data-stu-id="c9f63-299">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-300">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-300">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="c9f63-301">Entweder eine Zeichenfolge oder ein Wert der Ressourcen-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-301">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-302">Ressourcenfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-302">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="c9f63-303">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-303">Optional.</span></span> <span data-ttu-id="c9f63-304">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-304">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-305">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-305"></span></span> <span data-ttu-id="c9f63-306">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-306">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedDataAsync
    summary: '<span data-ttu-id="c9f63-307">Liest die Daten, die in der aktuellen Auswahl des Dokuments enthalten sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-307">Reads the data contained in the current selection in the document.</span></span>'
    remarks: |-
      <span data-ttu-id="c9f63-308">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-308">**Requirement sets**:</span></span>

      - <span data-ttu-id="c9f63-309">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (bei Verwendung von`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="c9f63-309">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="c9f63-310">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-310"></span></span>

      - <span data-ttu-id="c9f63-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (bei Verwendung von`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="c9f63-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="c9f63-312">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-312"></span></span>

      - <span data-ttu-id="c9f63-313">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (bei Verwendung von`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="c9f63-313">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="c9f63-314">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-314"></span></span>

      - [<span data-ttu-id="c9f63-315">Selection</span><span class="sxs-lookup"><span data-stu-id="c9f63-315">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="c9f63-316">[Umwandlung](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (bei Verwendung von`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="c9f63-316">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="c9f63-317">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-317"></span></span>

      - <span data-ttu-id="c9f63-318">[Textzwang](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (bei Verwendung von`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-318">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="c9f63-319">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-319"></span></span>

      <span data-ttu-id="c9f63-320">In der Callback-Funktion, die an die getSelectedDataAsync-Methode übergeben wird, können Sie die Eigenschaften des AsyncResult-Objekts verwenden, um die folgenden Informationen zurückzugeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-320">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-321">Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="c9f63-321">Property</span></span></th> <th><span data-ttu-id="c9f63-322">Verwendung</span><span class="sxs-lookup"><span data-stu-id="c9f63-322">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-323">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="c9f63-323">AsyncResult.value</span></span></td> <td><span data-ttu-id="c9f63-324">Gibt immer undefined zurück, weil kein Objekt oder Daten zum Abruf existieren.</span><span class="sxs-lookup"><span data-stu-id="c9f63-324">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-325">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="c9f63-325">AsyncResult.status</span></span></td> <td><span data-ttu-id="c9f63-326">Bestimmen Sie, ob der Vorgang erfolgreich war oder ein Fehler aufgetreten ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-326">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-327">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="c9f63-327">AsyncResult.error</span></span></td> <td><span data-ttu-id="c9f63-328">Greifen Sie auf ein Error-Objekt zu, das nach einem fehlgeschlagenen Vorgang Fehlerinformationen bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-328">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-329">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="c9f63-329">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="c9f63-330">Ein benutzerdefiniertes Element beliebigen Typs, das im AsyncResult-Objekt ohne Änderung zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-330">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="c9f63-331">Die möglichen Werte für den [Office. CoercionType](xref:office.Office.CoercionType) -Parameter variieren je nach Host.</span><span class="sxs-lookup"><span data-stu-id="c9f63-331">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-332">Host</span><span class="sxs-lookup"><span data-stu-id="c9f63-332">Host</span></span></th> <th><span data-ttu-id="c9f63-333">Unterstützter coercionType</span><span class="sxs-lookup"><span data-stu-id="c9f63-333">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-334">Excel, PowerPoint, Project und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-334">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="c9f63-335">`Office.CoercionType.Text`String</span><span class="sxs-lookup"><span data-stu-id="c9f63-335">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-336">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-336">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-337">`Office.CoercionType.Matrix`(Array von Arrays)</span><span class="sxs-lookup"><span data-stu-id="c9f63-337">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-338">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-338">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-339">`Office.CoercionType.Table`(Datenobjekt)</span><span class="sxs-lookup"><span data-stu-id="c9f63-339">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-340">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-340">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="c9f63-341">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-341">Word</span></span></td> <td><span data-ttu-id="c9f63-342">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="c9f63-342">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-343">PowerPoint im Internet und Windows</span><span class="sxs-lookup"><span data-stu-id="c9f63-343">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="c9f63-344">Excel, PowerPoint und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-344">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="c9f63-345">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-345">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: '<span data-ttu-id="c9f63-346">Der Typ der Datenstruktur, die zurückgegeben werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-346">The type of data structure to return.</span></span> <span data-ttu-id="c9f63-347">Weitere Informationen finden Sie im Abschnitt "Hinweise" für die unterstützten Umwandlungstypen der einzelnen Hosts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-347">See the remarks section for each host''s supported coercion types.</span></span>'
          type:
            - office.Office.CoercionType
        - id: options
          description: '<span data-ttu-id="c9f63-348">Enthält Optionen zum Anpassen der Daten, die zurückgegeben werden, und deren Formatierung.</span><span class="sxs-lookup"><span data-stu-id="c9f63-348">Provides options for customizing what data is returned and how it is formatted.</span></span>'
          type:
            - office.Office.GetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-349">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-349">Optional.</span></span> <span data-ttu-id="c9f63-350">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-350">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-351">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-351"></span></span> <span data-ttu-id="c9f63-352">Die `value` Eigenschaft des Ergebnisses sind die Daten in der aktuellen Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-352">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="c9f63-353">Dieser wird in der Datenstruktur oder dem Format zurückgegeben, die Sie mit dem coercionType-Parameter angegeben haben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-353">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="c9f63-354">(Weitere Informationen zur Datenumwandlung finden unter Hinweise.)</span><span class="sxs-lookup"><span data-stu-id="c9f63-354">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
      typeParameters:
        - id: T
  - uid: office.Office.Document.getSelectedDataAsync_1
    summary: '<span data-ttu-id="c9f63-355">Liest die Daten, die in der aktuellen Auswahl des Dokuments enthalten sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-355">Reads the data contained in the current selection in the document.</span></span>'
    remarks: |-
      <span data-ttu-id="c9f63-356">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-356">**Requirement sets**:</span></span>

      - <span data-ttu-id="c9f63-357">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (bei Verwendung von`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="c9f63-357">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="c9f63-358">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-358"></span></span>

      - <span data-ttu-id="c9f63-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (bei Verwendung von`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="c9f63-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="c9f63-360">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-360"></span></span>

      - <span data-ttu-id="c9f63-361">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (bei Verwendung von`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="c9f63-361">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="c9f63-362">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-362"></span></span>

      - [<span data-ttu-id="c9f63-363">Selection</span><span class="sxs-lookup"><span data-stu-id="c9f63-363">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="c9f63-364">[Umwandlung](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (bei Verwendung von`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="c9f63-364">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="c9f63-365">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-365"></span></span>

      - <span data-ttu-id="c9f63-366">[Textzwang](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (bei Verwendung von`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-366">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="c9f63-367">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-367"></span></span>

      <span data-ttu-id="c9f63-368">In der Callback-Funktion, die an die getSelectedDataAsync-Methode übergeben wird, können Sie die Eigenschaften des AsyncResult-Objekts verwenden, um die folgenden Informationen zurückzugeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-368">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-369">Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="c9f63-369">Property</span></span></th> <th><span data-ttu-id="c9f63-370">Verwendung</span><span class="sxs-lookup"><span data-stu-id="c9f63-370">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-371">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="c9f63-371">AsyncResult.value</span></span></td> <td><span data-ttu-id="c9f63-372">Gibt immer undefined zurück, weil kein Objekt oder Daten zum Abruf existieren.</span><span class="sxs-lookup"><span data-stu-id="c9f63-372">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-373">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="c9f63-373">AsyncResult.status</span></span></td> <td><span data-ttu-id="c9f63-374">Bestimmen Sie, ob der Vorgang erfolgreich war oder ein Fehler aufgetreten ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-374">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-375">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="c9f63-375">AsyncResult.error</span></span></td> <td><span data-ttu-id="c9f63-376">Greifen Sie auf ein Error-Objekt zu, das nach einem fehlgeschlagenen Vorgang Fehlerinformationen bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-376">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-377">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="c9f63-377">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="c9f63-378">Ein benutzerdefiniertes Element beliebigen Typs, das im AsyncResult-Objekt ohne Änderung zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-378">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="c9f63-379">Die möglichen Werte für den [Office. CoercionType](xref:office.Office.CoercionType) -Parameter variieren je nach Host.</span><span class="sxs-lookup"><span data-stu-id="c9f63-379">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-380">Host</span><span class="sxs-lookup"><span data-stu-id="c9f63-380">Host</span></span></th> <th><span data-ttu-id="c9f63-381">Unterstützter coercionType</span><span class="sxs-lookup"><span data-stu-id="c9f63-381">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-382">Excel, PowerPoint, Project und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-382">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="c9f63-383">`Office.CoercionType.Text`String</span><span class="sxs-lookup"><span data-stu-id="c9f63-383">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-384">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-384">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-385">`Office.CoercionType.Matrix`(Array von Arrays)</span><span class="sxs-lookup"><span data-stu-id="c9f63-385">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-386">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-386">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-387">`Office.CoercionType.Table`(Datenobjekt)</span><span class="sxs-lookup"><span data-stu-id="c9f63-387">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-388">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-388">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="c9f63-389">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-389">Word</span></span></td> <td><span data-ttu-id="c9f63-390">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="c9f63-390">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-391">PowerPoint im Internet und Windows</span><span class="sxs-lookup"><span data-stu-id="c9f63-391">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="c9f63-392">Excel, PowerPoint und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-392">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: '<span data-ttu-id="c9f63-393">Der Typ der Datenstruktur, die zurückgegeben werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-393">The type of data structure to return.</span></span> <span data-ttu-id="c9f63-394">Weitere Informationen finden Sie im Abschnitt "Hinweise" für die unterstützten Umwandlungstypen der einzelnen Hosts.</span><span class="sxs-lookup"><span data-stu-id="c9f63-394">See the remarks section for each host''s supported coercion types.</span></span>'
          type:
            - office.Office.CoercionType
        - id: callback
          description: '<span data-ttu-id="c9f63-395">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-395">Optional.</span></span> <span data-ttu-id="c9f63-396">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-396">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-397">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-397"></span></span> <span data-ttu-id="c9f63-398">Die `value` Eigenschaft des Ergebnisses sind die Daten in der aktuellen Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-398">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="c9f63-399">Dieser wird in der Datenstruktur oder dem Format zurückgegeben, die Sie mit dem coercionType-Parameter angegeben haben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-399">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="c9f63-400">(Weitere Informationen zur Datenumwandlung finden unter Hinweise.)</span><span class="sxs-lookup"><span data-stu-id="c9f63-400">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
      typeParameters:
        - id: T
  - uid: office.Office.Document.getSelectedResourceAsync
    summary: <span data-ttu-id="c9f63-401">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-401">Project documents only.</span></span> <span data-ttu-id="c9f63-402">Rufen Sie die ID der aktuell ausgewählten Ressource ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-402">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-403">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-403">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-404">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-404">Optional.</span></span> <span data-ttu-id="c9f63-405">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-405">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-406">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-406"></span></span> <span data-ttu-id="c9f63-407">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-407">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync_1
    summary: <span data-ttu-id="c9f63-408">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-408">Project documents only.</span></span> <span data-ttu-id="c9f63-409">Rufen Sie die ID der aktuell ausgewählten Ressource ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-409">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-410">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-410">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-411">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-411">Optional.</span></span> <span data-ttu-id="c9f63-412">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-412">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-413">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-413"></span></span> <span data-ttu-id="c9f63-414">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-414">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync
    summary: <span data-ttu-id="c9f63-415">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-415">Project documents only.</span></span> <span data-ttu-id="c9f63-416">Rufen Sie die ID des aktuell ausgewählten Vorgangs ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-416">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-417">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-417">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-418">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-418">Optional.</span></span> <span data-ttu-id="c9f63-419">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-419">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-420">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-420"></span></span> <span data-ttu-id="c9f63-421">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-421">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync_1
    summary: <span data-ttu-id="c9f63-422">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-422">Project documents only.</span></span> <span data-ttu-id="c9f63-423">Rufen Sie die ID des aktuell ausgewählten Vorgangs ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-423">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-424">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-424">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-425">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-425">Optional.</span></span> <span data-ttu-id="c9f63-426">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-426">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-427">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-427"></span></span> <span data-ttu-id="c9f63-428">Die `value` Eigenschaft des Ergebnisses ist die GUID der Ressource als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-428">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedViewAsync
    summary: <span data-ttu-id="c9f63-429">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-429">Project documents only.</span></span> <span data-ttu-id="c9f63-430">Abrufen des aktuellen ausgewählten Ansichtstyps (z. b.</span><span class="sxs-lookup"><span data-stu-id="c9f63-430">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="c9f63-431">Gantt) und Ansichts Name.</span><span class="sxs-lookup"><span data-stu-id="c9f63-431">Gantt) and View Name.</span></span>
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-432">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-432">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-433">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-433">Optional.</span></span> <span data-ttu-id="c9f63-434">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-434">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-435">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-435"></span></span> <span data-ttu-id="c9f63-436">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `viewName` -den Namen der Ansicht als ProjectViewTypes-Konstante.</span><span class="sxs-lookup"><span data-stu-id="c9f63-436">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="c9f63-437">`viewType`-Der Typ der Ansicht als ganzzahliger Wert einer ProjectViewTypes-Konstante.</span><span class="sxs-lookup"><span data-stu-id="c9f63-437">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getSelectedViewAsync_1
    summary: <span data-ttu-id="c9f63-438">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-438">Project documents only.</span></span> <span data-ttu-id="c9f63-439">Abrufen des aktuellen ausgewählten Ansichtstyps (z. b.</span><span class="sxs-lookup"><span data-stu-id="c9f63-439">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="c9f63-440">Gantt) und Ansichts Name.</span><span class="sxs-lookup"><span data-stu-id="c9f63-440">Gantt) and View Name.</span></span>
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-441">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-441">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-442">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-442">Optional.</span></span> <span data-ttu-id="c9f63-443">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-443">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-444">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-444"></span></span> <span data-ttu-id="c9f63-445">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `viewName` -den Namen der Ansicht als ProjectViewTypes-Konstante.</span><span class="sxs-lookup"><span data-stu-id="c9f63-445">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="c9f63-446">`viewType`-Der Typ der Ansicht als ganzzahliger Wert einer ProjectViewTypes-Konstante.</span><span class="sxs-lookup"><span data-stu-id="c9f63-446">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync
    summary: '<span data-ttu-id="c9f63-447">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-447">Project documents only.</span></span> <span data-ttu-id="c9f63-448">Rufen Sie den Vorgangsnamen, die WSS-Aufgaben-ID und ResourceNames für die angegebene Task-ID ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-448">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-449">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-449">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="c9f63-450">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-450">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-451">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-451">Optional.</span></span> <span data-ttu-id="c9f63-452">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-452">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-453">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-453"></span></span> <span data-ttu-id="c9f63-454">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `taskName` -den Namen der Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="c9f63-454">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="c9f63-455">`wssTaskId`– Die ID der Aufgabe in der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-455">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="c9f63-456">Wenn das Projekt nicht mit einer SharePoint-Aufgabenliste synchronisiert wird, ist der Wert 0.</span><span class="sxs-lookup"><span data-stu-id="c9f63-456">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="c9f63-457">`resourceNames`-Die durch trennzeichengetrennte Liste der Namen von Ressourcen, die dem Vorgang zugeordnet sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-457">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync_1
    summary: '<span data-ttu-id="c9f63-458">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-458">Project documents only.</span></span> <span data-ttu-id="c9f63-459">Rufen Sie den Vorgangsnamen, die WSS-Aufgaben-ID und ResourceNames für die angegebene Task-ID ab.</span><span class="sxs-lookup"><span data-stu-id="c9f63-459">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-460">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-460">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-461">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-461">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="c9f63-462">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-462">Optional.</span></span> <span data-ttu-id="c9f63-463">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-463">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-464">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-464"></span></span> <span data-ttu-id="c9f63-465">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `taskName` -den Namen der Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="c9f63-465">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="c9f63-466">`wssTaskId`– Die ID der Aufgabe in der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-466">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="c9f63-467">Wenn das Projekt nicht mit einer SharePoint-Aufgabenliste synchronisiert wird, ist der Wert 0.</span><span class="sxs-lookup"><span data-stu-id="c9f63-467">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="c9f63-468">`resourceNames`-Die durch trennzeichengetrennte Liste der Namen von Ressourcen, die dem Vorgang zugeordnet sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-468">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync
    summary: |-
      <span data-ttu-id="c9f63-469">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-469">Project documents only.</span></span> <span data-ttu-id="c9f63-470">Rufen Sie die GUID der Aufgabe ab, die den angegebenen Index in der Aufgabensammlung aufweist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-470">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="c9f63-471">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-471">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="c9f63-472">Der Index der Aufgabe in der Sammlung von Aufgaben für das Projekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-472">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="c9f63-473">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-473">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-474">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-474">Optional.</span></span> <span data-ttu-id="c9f63-475">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-475">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-476">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-476"></span></span> <span data-ttu-id="c9f63-477">Die `value` Eigenschaft des Ergebnisses ist die GUID der Aufgabe als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-477">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync_1
    summary: |-
      <span data-ttu-id="c9f63-478">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-478">Project documents only.</span></span> <span data-ttu-id="c9f63-479">Rufen Sie die GUID der Aufgabe ab, die den angegebenen Index in der Aufgabensammlung aufweist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-479">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="c9f63-480">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-480">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-481">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-481">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="c9f63-482">Der Index der Aufgabe in der Sammlung von Aufgaben für das Projekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-482">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="c9f63-483">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-483">Optional.</span></span> <span data-ttu-id="c9f63-484">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-484">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-485">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-485"></span></span> <span data-ttu-id="c9f63-486">Die `value` Eigenschaft des Ergebnisses ist die GUID der Aufgabe als Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="c9f63-486">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskFieldAsync
    summary: <span data-ttu-id="c9f63-487">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-487">Project documents only.</span></span> <span data-ttu-id="c9f63-488">Dient zum Abrufen des Vorgangsfelds für die angegebene Aufgaben-ID. (ex.</span><span class="sxs-lookup"><span data-stu-id="c9f63-488">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="c9f63-489">StartDate).</span><span class="sxs-lookup"><span data-stu-id="c9f63-489">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-490">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-490">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-491">Vorgangsfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-491">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="c9f63-492">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-492">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-493">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-493">Optional.</span></span> <span data-ttu-id="c9f63-494">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-494">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-495">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-495"></span></span> <span data-ttu-id="c9f63-496">Die `value` Eigenschaft des Ergebnisses enthält die `fieldValue` -Eigenschaft, die den Wert des angegebenen Felds darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-496">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskFieldAsync_1
    summary: <span data-ttu-id="c9f63-497">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-497">Project documents only.</span></span> <span data-ttu-id="c9f63-498">Dient zum Abrufen des Vorgangsfelds für die angegebene Aufgaben-ID. (ex.</span><span class="sxs-lookup"><span data-stu-id="c9f63-498">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="c9f63-499">StartDate).</span><span class="sxs-lookup"><span data-stu-id="c9f63-499">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-500">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-500">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-501">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-501">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-502">Vorgangsfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-502">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="c9f63-503">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-503">Optional.</span></span> <span data-ttu-id="c9f63-504">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-504">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-505">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-505"></span></span> <span data-ttu-id="c9f63-506">Die `value` Eigenschaft des Ergebnisses enthält die `fieldValue` -Eigenschaft, die den Wert des angegebenen Felds darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-506">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync
    summary: '<span data-ttu-id="c9f63-507">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-507">Project documents only.</span></span> <span data-ttu-id="c9f63-508">Abrufen der WSS-URL und des Listen namens für die Aufgabenliste wird auch das MPP synchronisiert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-508">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="c9f63-509">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-509">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-510">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-510">Optional.</span></span> <span data-ttu-id="c9f63-511">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-511">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-512">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-512"></span></span> <span data-ttu-id="c9f63-513">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `listName` -den Namen der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-513">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="c9f63-514">`serverUrl`– die URL der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-514">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync_1
    summary: '<span data-ttu-id="c9f63-515">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-515">Project documents only.</span></span> <span data-ttu-id="c9f63-516">Abrufen der WSS-URL und des Listen namens für die Aufgabenliste wird auch das MPP synchronisiert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-516">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="c9f63-517">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-517">Optional.</span></span> <span data-ttu-id="c9f63-518">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-518">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-519">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-519"></span></span> <span data-ttu-id="c9f63-520">Die `value` Eigenschaft des Ergebnisses enthält die folgenden Eigenschaften: `listName` -den Namen der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-520">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="c9f63-521">`serverUrl`– die URL der synchronisierten SharePoint-Aufgabenliste.</span><span class="sxs-lookup"><span data-stu-id="c9f63-521">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync
    summary: <span data-ttu-id="c9f63-522">Wechselt zum angegebenen Objekt oder Speicherort im Dokument.</span><span class="sxs-lookup"><span data-stu-id="c9f63-522">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-523">**Anforderungsgruppe**: [nicht in einer Gruppe](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="c9f63-523">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="c9f63-524">PowerPoint unterstützt die goToByIdAsync-Methode in Master Ansichten nicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-524">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="c9f63-525">Das durch die SelectionMode-Option verursachte Verhalten variiert je nach Host:</span><span class="sxs-lookup"><span data-stu-id="c9f63-525">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="c9f63-526">In Excel: `Office.SelectionMode.Selected` wählt alle Inhalte in der Bindung oder dem benannten Element aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-526">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="c9f63-527">Office.SelectionMode.Nonewählt für Textbindungen die Zelle aus; für Matrixbindungen, Tabellenbindungen und benannte Elemente wird die erste Datenzelle (nicht die erste Zell in der Kopfzeile für Tabellen) ausgewählt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-527">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="c9f63-528">In PowerPoint `Office.SelectionMode.Selected` : wählt den Folientitel oder das erste TextBox-Objekt auf der Folie aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-528">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="c9f63-529">`Office.SelectionMode.None`wählt nichts aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-529">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="c9f63-530">In Word: `Office.SelectionMode.Selected` wählt alle Inhalte in der Bindung aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-530">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="c9f63-531">Office.SelectionMode.None verschiebt für Textbindungen den Cursor an den Anfang des Textes; für Matrixbindungen und Tabellenbindungen wird die erste Datenzelle (nicht die erste Zelle in der Kopfzeile für Tabellen) ausgewählt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-531">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: '<span data-ttu-id="c9f63-532">Der Bezeichner des Objekts oder Speicherorts, zu dem gewechselt werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-532">The identifier of the object or location to go to.</span></span>'
          type:
            - string | number
        - id: goToType
          description: '<span data-ttu-id="c9f63-533">Der Typ des Speicherorts, zu dem gewechselt werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-533">The type of the location to go to.</span></span>'
          type:
            - office.Office.GoToType
        - id: options
          description: '<span data-ttu-id="c9f63-534">Enthält Optionen zum Auswählen des Standorts, zu dem navigiert wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-534">Provides options for whether to select the location that is navigated to.</span></span>'
          type:
            - office.Office.GoToByIdOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-535">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-535">Optional.</span></span> <span data-ttu-id="c9f63-536">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-536">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-537">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-537"></span></span> <span data-ttu-id="c9f63-538">Die `value` Eigenschaft des Ergebnisses ist die aktuelle Ansicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-538">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync_1
    summary: <span data-ttu-id="c9f63-539">Wechselt zum angegebenen Objekt oder Speicherort im Dokument.</span><span class="sxs-lookup"><span data-stu-id="c9f63-539">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-540">**Anforderungsgruppe**: [nicht in einer Gruppe](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="c9f63-540">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="c9f63-541">PowerPoint unterstützt die goToByIdAsync-Methode in Master Ansichten nicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-541">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="c9f63-542">Das durch die SelectionMode-Option verursachte Verhalten variiert je nach Host:</span><span class="sxs-lookup"><span data-stu-id="c9f63-542">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="c9f63-543">In Excel: `Office.SelectionMode.Selected` wählt alle Inhalte in der Bindung oder dem benannten Element aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-543">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="c9f63-544">Office.SelectionMode.Nonewählt für Textbindungen die Zelle aus; für Matrixbindungen, Tabellenbindungen und benannte Elemente wird die erste Datenzelle (nicht die erste Zell in der Kopfzeile für Tabellen) ausgewählt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-544">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="c9f63-545">In PowerPoint `Office.SelectionMode.Selected` : wählt den Folientitel oder das erste TextBox-Objekt auf der Folie aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-545">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="c9f63-546">`Office.SelectionMode.None`wählt nichts aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-546">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="c9f63-547">In Word: `Office.SelectionMode.Selected` wählt alle Inhalte in der Bindung aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-547">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="c9f63-548">Office.SelectionMode.None verschiebt für Textbindungen den Cursor an den Anfang des Textes; für Matrixbindungen und Tabellenbindungen wird die erste Datenzelle (nicht die erste Zelle in der Kopfzeile für Tabellen) ausgewählt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-548">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-549">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-549">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: '<span data-ttu-id="c9f63-550">Der Bezeichner des Objekts oder Speicherorts, zu dem gewechselt werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-550">The identifier of the object or location to go to.</span></span>'
          type:
            - string | number
        - id: goToType
          description: '<span data-ttu-id="c9f63-551">Der Typ des Speicherorts, zu dem gewechselt werden soll.</span><span class="sxs-lookup"><span data-stu-id="c9f63-551">The type of the location to go to.</span></span>'
          type:
            - office.Office.GoToType
        - id: callback
          description: '<span data-ttu-id="c9f63-552">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-552">Optional.</span></span> <span data-ttu-id="c9f63-553">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-553">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-554">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-554"></span></span> <span data-ttu-id="c9f63-555">Die `value` Eigenschaft des Ergebnisses ist die aktuelle Ansicht.</span><span class="sxs-lookup"><span data-stu-id="c9f63-555">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.removeHandlerAsync
    summary: <span data-ttu-id="c9f63-556">Entfernt einen Ereignishandler für den angegebenen Ereignistyp.</span><span class="sxs-lookup"><span data-stu-id="c9f63-556">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="c9f63-557">**Anforderungsgruppe**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="c9f63-557">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="c9f63-558">Der Ereignistyp.</span><span class="sxs-lookup"><span data-stu-id="c9f63-558">The event type.</span></span> <span data-ttu-id="c9f63-559">Für Document kann "Document. SelectionChanged" oder "Document. ActiveViewChanged" sein.</span><span class="sxs-lookup"><span data-stu-id="c9f63-559">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - office.Office.EventType
        - id: options
          description: '<span data-ttu-id="c9f63-560">Enthält Optionen zum bestimmen, welche Ereignishandler oder Handler entfernt werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-560">Provides options to determine which event handler or handlers are removed.</span></span>'
          type:
            - office.Office.RemoveHandlerOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-561">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-561">Optional.</span></span> <span data-ttu-id="c9f63-562">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-562">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-563">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-563"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.removeHandlerAsync_1
    summary: <span data-ttu-id="c9f63-564">Entfernt einen Ereignishandler für den angegebenen Ereignistyp.</span><span class="sxs-lookup"><span data-stu-id="c9f63-564">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-565">**Anforderungsgruppe**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="c9f63-565">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-566">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-566">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="c9f63-567">Der Ereignistyp.</span><span class="sxs-lookup"><span data-stu-id="c9f63-567">The event type.</span></span> <span data-ttu-id="c9f63-568">Für Document kann "Document. SelectionChanged" oder "Document. ActiveViewChanged" sein.</span><span class="sxs-lookup"><span data-stu-id="c9f63-568">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - office.Office.EventType
        - id: callback
          description: '<span data-ttu-id="c9f63-569">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-569">Optional.</span></span> <span data-ttu-id="c9f63-570">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-570">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-571">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-571"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync
    summary: |-
      <span data-ttu-id="c9f63-572">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-572">Project documents only.</span></span> <span data-ttu-id="c9f63-573">Ressourcenfeld für angegebene Ressourcen-ID festlegen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-573">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="c9f63-574">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-574">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="c9f63-575">Entweder eine Zeichenfolge oder ein Wert der Ressourcen-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-575">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-576">Ressourcenfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-576">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="c9f63-577">Wert des Ziel Felds.</span><span class="sxs-lookup"><span data-stu-id="c9f63-577">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="c9f63-578">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-578">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-579">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-579">Optional.</span></span> <span data-ttu-id="c9f63-580">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-580">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-581">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-581"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync_1
    summary: |-
      <span data-ttu-id="c9f63-582">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-582">Project documents only.</span></span> <span data-ttu-id="c9f63-583">Ressourcenfeld für angegebene Ressourcen-ID festlegen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-583">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="c9f63-584">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-584">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-585">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-585">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="c9f63-586">Entweder eine Zeichenfolge oder ein Wert der Ressourcen-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-586">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-587">Ressourcenfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-587">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="c9f63-588">Wert des Ziel Felds.</span><span class="sxs-lookup"><span data-stu-id="c9f63-588">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="c9f63-589">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-589">Optional.</span></span> <span data-ttu-id="c9f63-590">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-590">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-591">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-591"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync
    summary: <span data-ttu-id="c9f63-592">Schreibt die angegebenen Daten in die aktuelle Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-592">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-593">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-593">**Requirement sets**:</span></span>

      - [<span data-ttu-id="c9f63-594">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="c9f63-594">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="c9f63-595">(bei Verwendung von`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="c9f63-595">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="c9f63-596">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-596"></span></span>

      - <span data-ttu-id="c9f63-597">[Imageerzwingung 1,1](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (bei Verwendung von`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="c9f63-597">[ImageCoercion 1.1](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="c9f63-598">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-598"></span></span>

      - <span data-ttu-id="c9f63-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (bei Verwendung von`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="c9f63-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="c9f63-600">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-600"></span></span>

      - <span data-ttu-id="c9f63-601">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (bei Verwendung von`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="c9f63-601">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="c9f63-602">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-602"></span></span>

      - [<span data-ttu-id="c9f63-603">Selection</span><span class="sxs-lookup"><span data-stu-id="c9f63-603">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="c9f63-604">[Umwandlung](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (bei Verwendung von`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="c9f63-604">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="c9f63-605">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-605"></span></span>

      - <span data-ttu-id="c9f63-606">[Textzwang](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (bei Verwendung von`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-606">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="c9f63-607">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-607"></span></span>

      - <span data-ttu-id="c9f63-608">[Imageerzwingung 1,2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (bei Verwendung von`Office.CoercionType.XmlSvg`</span><span class="sxs-lookup"><span data-stu-id="c9f63-608">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (when using `Office.CoercionType.XmlSvg`</span></span><!-- --><span data-ttu-id="c9f63-609">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-609"></span></span>

      <span data-ttu-id="c9f63-610">**Anwendungsspezifische Verhaltensweisen**</span><span class="sxs-lookup"><span data-stu-id="c9f63-610">**Application-specific behaviors**</span></span>

      <span data-ttu-id="c9f63-611">Die folgenden anwendungsspezifischen Aktionen gelten beim Schreiben von Daten in eine Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-611">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="c9f63-612">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-612">Word</span></span></td> <td><span data-ttu-id="c9f63-613">Wenn keine Auswahl vorhanden ist und sich die Einfügemarke an einer gültigen Position befindet, wird das `data` angegebene an der Einfügemarke eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-613">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="c9f63-614">Wenn `data` es sich um eine Zeichenfolge handelt, wird der angegebene Text eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-614">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-615">Wenn `data` es sich um ein Array von Arrays ("Matrix") oder ein Tabellendaten Objekt handelt, wird eine neue Word-Tabelle eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-615">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-616">Wenn `data` es sich um HTML handelt, wird der angegebene HTML-Code eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-616">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="c9f63-617">(**Wichtig**: Wenn einer der von Ihnen eingefügten HTML-Code ungültig ist, löst Word keinen Fehler aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-617">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="c9f63-618">Word wird so viel HTML wie möglich einfügen und alle ungültigen Daten weglassen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-618">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-619">Wenn `data` Office Open XML ist, wird der angegebene XML-Code eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-619">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-620">Wenn `data` es sich um einen Base64-codierten Bilddatenstrom handelt, wird das angegebene Bild eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-620">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-621">Wenn eine Auswahl vorhanden ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-621">If there is a selection</span></span></td> <td><span data-ttu-id="c9f63-622">Es wird durch das angegebene `data` nach den gleichen Regeln wie oben ersetzt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-622">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-623">Einfügen von Bildern</span><span class="sxs-lookup"><span data-stu-id="c9f63-623">Insert images</span></span></td> <td><span data-ttu-id="c9f63-624">Eingefügte Bilder werden Inline platziert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-624">Inserted images are placed inline.</span></span> <span data-ttu-id="c9f63-625">Die Parameter imageLeft und imageTop werden ignoriert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-625">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="c9f63-626">Das Bildseitenverhältnis ist immer gesperrt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-626">The image aspect ratio is always locked.</span></span> <span data-ttu-id="c9f63-627">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-627">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-628">Excel</span><span class="sxs-lookup"><span data-stu-id="c9f63-628">Excel</span></span></td> <td><span data-ttu-id="c9f63-629">Wenn eine einzelne Zelle ausgewählt ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-629">If a single cell is selected</span></span></td> <td><span data-ttu-id="c9f63-630">Wenn `data` es sich um eine Zeichenfolge handelt, wird der angegebene Text als Wert der aktuellen Zelle eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-630">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-631">Wenn `data` es sich um ein Array von Arrays ("Matrix") handelt, wird die angegebene Gruppe von Zeilen und Spalten eingefügt, wenn keine anderen Daten in den umliegenden Zellen überschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-631">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-632">Wenn `data` es sich um ein Tabellendaten Objekt handelt, wird eine neue Excel-Tabelle mit dem angegebenen Zeilen-und Kopfzeilentyp eingefügt, wenn keine anderen Daten in den umliegenden Zellen überschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-632">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-633">Wenn mehrere Zellen ausgewählt sind</span><span class="sxs-lookup"><span data-stu-id="c9f63-633">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="c9f63-634">, wird ein Fehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-634">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="c9f63-635">, werden die Werte der ausgewählten Zellen basierend auf den Werten in aktualisiert.`data`</span><span class="sxs-lookup"><span data-stu-id="c9f63-635">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="c9f63-636">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-636"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-637">Einfügen von Bildern</span><span class="sxs-lookup"><span data-stu-id="c9f63-637">Insert images</span></span></td> <td><span data-ttu-id="c9f63-638">Eingefügte Bilder sind schwebend.</span><span class="sxs-lookup"><span data-stu-id="c9f63-638">Inserted images are floating.</span></span> <span data-ttu-id="c9f63-639">Die Positionsparameter imageLeft und imageTop sind relativ zu den aktuell ausgewählten Zellen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-639">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="c9f63-640">Negative Werte für imageLeft und imageTop sind zulässig und werden möglicherweise  von Excel an die Bildposition innerhalb eines Arbeitsblatts angepasst.</span><span class="sxs-lookup"><span data-stu-id="c9f63-640">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="c9f63-641">Das Bild-Seitenverhältnis ist gesperrt, es sei denn, die beiden Parameter imageWidth und imageHeight werden angegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-641">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="c9f63-642">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-642">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-643">Alle anderen Fälle</span><span class="sxs-lookup"><span data-stu-id="c9f63-643">All other cases</span></span></td> <td><span data-ttu-id="c9f63-644">Es wird ein Fehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-644">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-645">Excel im Internet</span><span class="sxs-lookup"><span data-stu-id="c9f63-645">Excel on the web</span></span></td> <td><span data-ttu-id="c9f63-646">Zusätzlich zu den oben beschriebenen Verhaltensweisen für Excel gelten diese Grenzwerte beim Schreiben von Daten in Excel im Internet.</span><span class="sxs-lookup"><span data-stu-id="c9f63-646">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="c9f63-647">Die Gesamtzahl der Zellen, die Sie in ein Arbeitsblatt mit `data` dem Parameter schreiben können, darf 20.000 in einem einzigen Aufruf dieser Methode nicht überschreiten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-647">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-648">Die Anzahl der Formatierungs Gruppen, die `cellFormat` an den Parameter übergeben werden, darf 100 nicht überschreiten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-648">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="c9f63-649">Eine einzelne Formatierungsgruppe besteht aus einem Formatierungssatz, der auf einen bestimmten Zellenbereich angewendet wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-649">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-650">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="c9f63-650">PowerPoint</span></span></td> <td><span data-ttu-id="c9f63-651">Bild einfügen</span><span class="sxs-lookup"><span data-stu-id="c9f63-651">Insert image</span></span></td> <td><span data-ttu-id="c9f63-652">Eingefügte Bilder sind schwebend.</span><span class="sxs-lookup"><span data-stu-id="c9f63-652">Inserted images are floating.</span></span> <span data-ttu-id="c9f63-653">Die Parameter Position imageleft und imageTop sind optional, aber wenn angegeben, sollten beide vorhanden sein.</span><span class="sxs-lookup"><span data-stu-id="c9f63-653">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="c9f63-654">Wenn ein einzelner Wert bereitgestellt wird, wird er ignoriert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-654">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="c9f63-655">Negative Wert für imageLeft und imageTop sind zulässig und können ein Bild außerhalb einer Folie positionieren.</span><span class="sxs-lookup"><span data-stu-id="c9f63-655">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="c9f63-656">Wenn keine optionaler Parameter angegeben ist und die Folie einen Platzhalter aufweist, ersetzt das Bild den Platzhalter auf der Folie.</span><span class="sxs-lookup"><span data-stu-id="c9f63-656">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="c9f63-657">Das Bild-Seitenverhältnis wird gesperrt, es sei denn, die beiden Parameter imageWidth und imageHeight werden angegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-657">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="c9f63-658">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-658">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="c9f63-659">Die möglichen Werte für den [Office. CoercionType](xref:office.Office.CoercionType) -Parameter variieren je nach Host.</span><span class="sxs-lookup"><span data-stu-id="c9f63-659">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-660">Host</span><span class="sxs-lookup"><span data-stu-id="c9f63-660">Host</span></span></th> <th><span data-ttu-id="c9f63-661">Unterstützter coercionType</span><span class="sxs-lookup"><span data-stu-id="c9f63-661">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-662">Excel, PowerPoint, Project und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-662">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="c9f63-663">`Office.CoercionType.Text`String</span><span class="sxs-lookup"><span data-stu-id="c9f63-663">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-664">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-664">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-665">`Office.CoercionType.Matrix`(Array von Arrays)</span><span class="sxs-lookup"><span data-stu-id="c9f63-665">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-666">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-666">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-667">`Office.CoercionType.Table`(Datenobjekt)</span><span class="sxs-lookup"><span data-stu-id="c9f63-667">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-668">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-668">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="c9f63-669">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-669">Word</span></span></td> <td><span data-ttu-id="c9f63-670">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="c9f63-670">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-671">PowerPoint im Internet und Windows</span><span class="sxs-lookup"><span data-stu-id="c9f63-671">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="c9f63-672">Excel, PowerPoint und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-672">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="c9f63-673">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-673">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="c9f63-674">Die festzulegenden Daten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-674">The data to be set.</span></span> <span data-ttu-id="c9f63-675">Entweder eine Zeichenfolge oder ein [Office. CoercionType](xref:office.Office.CoercionType) -Wert, ein 2D-Array oder ein Datenobjekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-675">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="c9f63-676">Wenn der übergebene Wert `data` lautet:</span><span class="sxs-lookup"><span data-stu-id="c9f63-676">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="c9f63-677">Eine Zeichenfolge: Nur-Text oder alle Werte, die in eine Zeichenfolge umgewandelt werden können, werden eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-677">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="c9f63-678">In Excel können Sie auch Daten als gültige Formel angeben, um diese Formel zur ausgewählten Zelle hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-678">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="c9f63-679">Indem Sie den Wert data auf "=SUM(A1:A5)" setzen, wird das Ergebnis der Werte des angegebenen Bereichs angezeigt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-679">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="c9f63-680">Wenn Sie jedoch eine Formel für die gebundene Zelle festlegen, können Sie die hinzugefügte Formel (oder eine bereits vorhandene Formel) anschließend nicht mehr aus der gebundenen Zelle lesen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-680">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="c9f63-681">Wenn Sie die Document.getSelectedDataAsyncMethode in der ausgewählten Zelle aufrufen, um die Daten zu lesen, kann die Methode nur die in der Zelle angezeigten Daten (das Formelergebnis) zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-681">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="c9f63-682">Ein Array von Arrays („Matrix“): Es werden tabellarische Daten ohne Kopfzeilen eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-682">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="c9f63-683">Um beispielsweise Daten in drei Zeilen in zwei Spalten zu schreiben, können Sie ein Array wie das folgende übergeben \[ \[: "Z1S1", "R1C2\]" \[, "R2C1", "R2C2\]" \[, "R3C1", "R3C2\]\]".</span><span class="sxs-lookup"><span data-stu-id="c9f63-683">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="c9f63-684">Wenn Sie eine einzelne Spalte aus drei Zeilen schreiben möchten, übergeben Sie ein Array \[ \[wie das folgende\]: \["Z1S1"\], \["R2C1", "R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="c9f63-684">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="c9f63-685">In Excel können Sie auch  data als ein Array von Arrays mit gültigen Formeln angeben, um diese Formeln zu den ausgewählten Zellen hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-685">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="c9f63-686">Wenn beispielsweise keine anderen Daten überschrieben werden, werden durch Festlegen \[ \[von Daten auf "= Sum (a1: A5)", "= Average (a1:\] \] a5)" diese beiden Formeln zur Auswahl hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-686">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="c9f63-687">Genau wie beim Angeben einer Formel für eine einzelne Zelle als „Text“, können Sie die hinzugefügten Formeln (oder bereits vorhandene Formeln) anschließend nicht mehr lesen. Sie können nur die Formelergebnisse lesen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-687">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="c9f63-688">Ein TableData-Objekt: Eine Tabelle mit Kopfzeilen wird eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-688">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="c9f63-689">Wenn Sie in Excel die Formeln im TableData-Objekt angeben, dass Sie für den data-Parameter weitergeben, erhalten Sie wegen der "berechnete Spalten"-Funktion möglicherweise nicht die erwarteten Ergebnisse, die die Formeln innerhalb einer Spalte automatisch dupliziert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-689">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="c9f63-690">Um dies zu umgehen, wenn Sie schreiben `data` möchten, das Formeln in einer ausgewählten Tabelle enthält, versuchen Sie, die Daten als Array von Arrays (anstelle eines tabledaten-Objekts) anzugeben, und geben Sie die coercionType als Microsoft. Office. Matrix oder "Matrix" an.</span><span class="sxs-lookup"><span data-stu-id="c9f63-690">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="c9f63-691">Dieses Verfahren blockiert jedoch das Feature "berechnete Spalten" nur, wenn eine der folgenden Bedingungen erfüllt ist: (1) Sie schreiben in alle Zellen der Spalte, oder (2) Es gibt bereits mindestens zwei unterschiedliche Formeln in der Spalte.</span><span class="sxs-lookup"><span data-stu-id="c9f63-691">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: options
          description: <span data-ttu-id="c9f63-692">Enthält Optionen zum Einfügen von Daten in die Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-692">Provides options for how to insert data to the selection.</span></span>
          type:
            - office.Office.SetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-693">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-693">Optional.</span></span> <span data-ttu-id="c9f63-694">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-694">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-695">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-695"></span></span> <span data-ttu-id="c9f63-696">Die AsyncResult. Value-Eigenschaft gibt immer undefined zurück, da kein Objekt oder Daten zum Abrufen vorhanden sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-696">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync_1
    summary: <span data-ttu-id="c9f63-697">Schreibt die angegebenen Daten in die aktuelle Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-697">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="c9f63-698">**Anforderungs Sätze**:</span><span class="sxs-lookup"><span data-stu-id="c9f63-698">**Requirement sets**:</span></span>

      - [<span data-ttu-id="c9f63-699">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="c9f63-699">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="c9f63-700">(bei Verwendung von`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="c9f63-700">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="c9f63-701">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-701"></span></span>

      - <span data-ttu-id="c9f63-702">[Imagezwang](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (bei Verwendung von`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="c9f63-702">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="c9f63-703">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-703"></span></span>

      - <span data-ttu-id="c9f63-704">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (bei Verwendung von`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="c9f63-704">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="c9f63-705">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-705"></span></span>

      - <span data-ttu-id="c9f63-706">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (bei Verwendung von`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="c9f63-706">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="c9f63-707">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-707"></span></span>

      - [<span data-ttu-id="c9f63-708">Selection</span><span class="sxs-lookup"><span data-stu-id="c9f63-708">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="c9f63-709">[Umwandlung](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (bei Verwendung von`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="c9f63-709">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="c9f63-710">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-710"></span></span>

      - <span data-ttu-id="c9f63-711">[Textzwang](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (bei Verwendung von`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="c9f63-711">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="c9f63-712">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-712"></span></span>

      - <span data-ttu-id="c9f63-713">[Imageerzwingung 1,2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (bei Verwendung von`Office.CoercionType.XmlSvg`</span><span class="sxs-lookup"><span data-stu-id="c9f63-713">[ImageCoercion 1.2](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/image-coercion-requirement-sets#imagecoercion-12) (when using `Office.CoercionType.XmlSvg`</span></span><!-- --><span data-ttu-id="c9f63-714">)</span><span class="sxs-lookup"><span data-stu-id="c9f63-714"></span></span>

      <span data-ttu-id="c9f63-715">**Anwendungsspezifische Verhaltensweisen**</span><span class="sxs-lookup"><span data-stu-id="c9f63-715">**Application-specific behaviors**</span></span>

      <span data-ttu-id="c9f63-716">Die folgenden anwendungsspezifischen Aktionen gelten beim Schreiben von Daten in eine Auswahl.</span><span class="sxs-lookup"><span data-stu-id="c9f63-716">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="c9f63-717">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-717">Word</span></span></td> <td><span data-ttu-id="c9f63-718">Wenn keine Auswahl vorhanden ist und sich die Einfügemarke an einer gültigen Position befindet, wird das `data` angegebene an der Einfügemarke eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-718">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="c9f63-719">Wenn `data` es sich um eine Zeichenfolge handelt, wird der angegebene Text eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-719">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-720">Wenn `data` es sich um ein Array von Arrays ("Matrix") oder ein Tabellendaten Objekt handelt, wird eine neue Word-Tabelle eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-720">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-721">Wenn `data` es sich um HTML handelt, wird der angegebene HTML-Code eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-721">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="c9f63-722">(**Wichtig**: Wenn einer der von Ihnen eingefügten HTML-Code ungültig ist, löst Word keinen Fehler aus.</span><span class="sxs-lookup"><span data-stu-id="c9f63-722">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="c9f63-723">Word wird so viel HTML wie möglich einfügen und alle ungültigen Daten weglassen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-723">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-724">Wenn `data` Office Open XML ist, wird der angegebene XML-Code eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-724">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-725">Wenn `data` es sich um einen Base64-codierten Bilddatenstrom handelt, wird das angegebene Bild eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-725">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-726">Wenn eine Auswahl vorhanden ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-726">If there is a selection</span></span></td> <td><span data-ttu-id="c9f63-727">Es wird durch das angegebene `data` nach den gleichen Regeln wie oben ersetzt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-727">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-728">Einfügen von Bildern</span><span class="sxs-lookup"><span data-stu-id="c9f63-728">Insert images</span></span></td> <td><span data-ttu-id="c9f63-729">Eingefügte Bilder werden Inline platziert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-729">Inserted images are placed inline.</span></span> <span data-ttu-id="c9f63-730">Die Parameter imageLeft und imageTop werden ignoriert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-730">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="c9f63-731">Das Bildseitenverhältnis ist immer gesperrt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-731">The image aspect ratio is always locked.</span></span> <span data-ttu-id="c9f63-732">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-732">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-733">Excel</span><span class="sxs-lookup"><span data-stu-id="c9f63-733">Excel</span></span></td> <td><span data-ttu-id="c9f63-734">Wenn eine einzelne Zelle ausgewählt ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-734">If a single cell is selected</span></span></td> <td><span data-ttu-id="c9f63-735">Wenn `data` es sich um eine Zeichenfolge handelt, wird der angegebene Text als Wert der aktuellen Zelle eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-735">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-736">Wenn `data` es sich um ein Array von Arrays ("Matrix") handelt, wird die angegebene Gruppe von Zeilen und Spalten eingefügt, wenn keine anderen Daten in den umliegenden Zellen überschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-736">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-737">Wenn `data` es sich um ein Tabellendaten Objekt handelt, wird eine neue Excel-Tabelle mit dem angegebenen Zeilen-und Kopfzeilentyp eingefügt, wenn keine anderen Daten in den umliegenden Zellen überschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="c9f63-737">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-738">Wenn mehrere Zellen ausgewählt sind</span><span class="sxs-lookup"><span data-stu-id="c9f63-738">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="c9f63-739">, wird ein Fehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-739">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="c9f63-740">, werden die Werte der ausgewählten Zellen basierend auf den Werten in aktualisiert.`data`</span><span class="sxs-lookup"><span data-stu-id="c9f63-740">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="c9f63-741">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-741"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-742">Einfügen von Bildern</span><span class="sxs-lookup"><span data-stu-id="c9f63-742">Insert images</span></span></td> <td><span data-ttu-id="c9f63-743">Eingefügte Bilder sind schwebend.</span><span class="sxs-lookup"><span data-stu-id="c9f63-743">Inserted images are floating.</span></span> <span data-ttu-id="c9f63-744">Die Positionsparameter imageLeft und imageTop sind relativ zu den aktuell ausgewählten Zellen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-744">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="c9f63-745">Negative Werte für imageLeft und imageTop sind zulässig und werden möglicherweise  von Excel an die Bildposition innerhalb eines Arbeitsblatts angepasst.</span><span class="sxs-lookup"><span data-stu-id="c9f63-745">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="c9f63-746">Das Bild-Seitenverhältnis ist gesperrt, es sei denn, die beiden Parameter imageWidth und imageHeight werden angegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-746">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="c9f63-747">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-747">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="c9f63-748">Alle anderen Fälle</span><span class="sxs-lookup"><span data-stu-id="c9f63-748">All other cases</span></span></td> <td><span data-ttu-id="c9f63-749">Es wird ein Fehler zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-749">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-750">Excel im Internet</span><span class="sxs-lookup"><span data-stu-id="c9f63-750">Excel on the web</span></span></td> <td><span data-ttu-id="c9f63-751">Zusätzlich zu den oben beschriebenen Verhaltensweisen für Excel gelten diese Grenzwerte beim Schreiben von Daten in Excel im Internet.</span><span class="sxs-lookup"><span data-stu-id="c9f63-751">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="c9f63-752">Die Gesamtzahl der Zellen, die Sie in ein Arbeitsblatt mit `data` dem Parameter schreiben können, darf 20.000 in einem einzigen Aufruf dieser Methode nicht überschreiten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-752">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="c9f63-753">Die Anzahl der Formatierungs Gruppen, die `cellFormat` an den Parameter übergeben werden, darf 100 nicht überschreiten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-753">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="c9f63-754">Eine einzelne Formatierungsgruppe besteht aus einem Formatierungssatz, der auf einen bestimmten Zellenbereich angewendet wird.</span><span class="sxs-lookup"><span data-stu-id="c9f63-754">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="c9f63-755">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="c9f63-755">PowerPoint</span></span></td> <td><span data-ttu-id="c9f63-756">Bild einfügen</span><span class="sxs-lookup"><span data-stu-id="c9f63-756">Insert image</span></span></td> <td><span data-ttu-id="c9f63-757">Eingefügte Bilder sind schwebend.</span><span class="sxs-lookup"><span data-stu-id="c9f63-757">Inserted images are floating.</span></span> <span data-ttu-id="c9f63-758">Die Parameter Position imageleft und imageTop sind optional, aber wenn angegeben, sollten beide vorhanden sein.</span><span class="sxs-lookup"><span data-stu-id="c9f63-758">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="c9f63-759">Wenn ein einzelner Wert bereitgestellt wird, wird er ignoriert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-759">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="c9f63-760">Negative Wert für imageLeft und imageTop sind zulässig und können ein Bild außerhalb einer Folie positionieren.</span><span class="sxs-lookup"><span data-stu-id="c9f63-760">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="c9f63-761">Wenn keine optionaler Parameter angegeben ist und die Folie einen Platzhalter aufweist, ersetzt das Bild den Platzhalter auf der Folie.</span><span class="sxs-lookup"><span data-stu-id="c9f63-761">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="c9f63-762">Das Bild-Seitenverhältnis wird gesperrt, es sei denn, die beiden Parameter imageWidth und imageHeight werden angegeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-762">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="c9f63-763">Wenn nur einer der Parameter imageWidth und imageHeight angegeben ist, wird der andere Wert automatisch skaliert, um das ursprüngliche Seitenverhältnis beizubehalten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-763">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="c9f63-764">Die möglichen Werte für den [Office. CoercionType](xref:office.Office.CoercionType) -Parameter variieren je nach Host.</span><span class="sxs-lookup"><span data-stu-id="c9f63-764">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="c9f63-765">Host</span><span class="sxs-lookup"><span data-stu-id="c9f63-765">Host</span></span></th> <th><span data-ttu-id="c9f63-766">Unterstützter coercionType</span><span class="sxs-lookup"><span data-stu-id="c9f63-766">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="c9f63-767">Excel, PowerPoint, Project und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-767">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="c9f63-768">`Office.CoercionType.Text`String</span><span class="sxs-lookup"><span data-stu-id="c9f63-768">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-769">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-769">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-770">`Office.CoercionType.Matrix`(Array von Arrays)</span><span class="sxs-lookup"><span data-stu-id="c9f63-770">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-771">Excel und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-771">Excel and Word</span></span></td> <td><span data-ttu-id="c9f63-772">`Office.CoercionType.Table`(Datenobjekt)</span><span class="sxs-lookup"><span data-stu-id="c9f63-772">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-773">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-773">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="c9f63-774">Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-774">Word</span></span></td> <td><span data-ttu-id="c9f63-775">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="c9f63-775">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="c9f63-776">PowerPoint im Internet und Windows</span><span class="sxs-lookup"><span data-stu-id="c9f63-776">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="c9f63-777">Excel, PowerPoint und Word</span><span class="sxs-lookup"><span data-stu-id="c9f63-777">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="c9f63-778">Die festzulegenden Daten.</span><span class="sxs-lookup"><span data-stu-id="c9f63-778">The data to be set.</span></span> <span data-ttu-id="c9f63-779">Entweder eine Zeichenfolge oder ein [Office. CoercionType](xref:office.Office.CoercionType) -Wert, ein 2D-Array oder ein Datenobjekt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-779">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="c9f63-780">Wenn der übergebene Wert `data` lautet:</span><span class="sxs-lookup"><span data-stu-id="c9f63-780">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="c9f63-781">Eine Zeichenfolge: Nur-Text oder alle Werte, die in eine Zeichenfolge umgewandelt werden können, werden eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-781">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="c9f63-782">In Excel können Sie auch Daten als gültige Formel angeben, um diese Formel zur ausgewählten Zelle hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-782">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="c9f63-783">Indem Sie den Wert data auf "=SUM(A1:A5)" setzen, wird das Ergebnis der Werte des angegebenen Bereichs angezeigt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-783">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="c9f63-784">Wenn Sie jedoch eine Formel für die gebundene Zelle festlegen, können Sie die hinzugefügte Formel (oder eine bereits vorhandene Formel) anschließend nicht mehr aus der gebundenen Zelle lesen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-784">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="c9f63-785">Wenn Sie die Document.getSelectedDataAsyncMethode in der ausgewählten Zelle aufrufen, um die Daten zu lesen, kann die Methode nur die in der Zelle angezeigten Daten (das Formelergebnis) zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="c9f63-785">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="c9f63-786">Ein Array von Arrays („Matrix“): Es werden tabellarische Daten ohne Kopfzeilen eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-786">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="c9f63-787">Um beispielsweise Daten in drei Zeilen in zwei Spalten zu schreiben, können Sie ein Array wie das folgende übergeben \[ \[: "Z1S1", "R1C2\]" \[, "R2C1", "R2C2\]" \[, "R3C1", "R3C2\]\]".</span><span class="sxs-lookup"><span data-stu-id="c9f63-787">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="c9f63-788">Wenn Sie eine einzelne Spalte aus drei Zeilen schreiben möchten, übergeben Sie ein Array \[ \[wie das folgende\]: \["Z1S1"\], \["R2C1", "R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="c9f63-788">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="c9f63-789">In Excel können Sie auch  data als ein Array von Arrays mit gültigen Formeln angeben, um diese Formeln zu den ausgewählten Zellen hinzuzufügen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-789">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="c9f63-790">Wenn beispielsweise keine anderen Daten überschrieben werden, werden durch Festlegen \[ \[von Daten auf "= Sum (a1: A5)", "= Average (a1:\] \] a5)" diese beiden Formeln zur Auswahl hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-790">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="c9f63-791">Genau wie beim Angeben einer Formel für eine einzelne Zelle als „Text“, können Sie die hinzugefügten Formeln (oder bereits vorhandene Formeln) anschließend nicht mehr lesen. Sie können nur die Formelergebnisse lesen.</span><span class="sxs-lookup"><span data-stu-id="c9f63-791">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="c9f63-792">Ein TableData-Objekt: Eine Tabelle mit Kopfzeilen wird eingefügt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-792">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="c9f63-793">Wenn Sie in Excel die Formeln im TableData-Objekt angeben, dass Sie für den data-Parameter weitergeben, erhalten Sie wegen der "berechnete Spalten"-Funktion möglicherweise nicht die erwarteten Ergebnisse, die die Formeln innerhalb einer Spalte automatisch dupliziert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-793">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="c9f63-794">Um dies zu umgehen, wenn Sie schreiben `data` möchten, das Formeln in einer ausgewählten Tabelle enthält, versuchen Sie, die Daten als Array von Arrays (anstelle eines tabledaten-Objekts) anzugeben, und geben Sie die coercionType als Microsoft. Office. Matrix oder "Matrix" an.</span><span class="sxs-lookup"><span data-stu-id="c9f63-794">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="c9f63-795">Dieses Verfahren blockiert jedoch das Feature "berechnete Spalten" nur, wenn eine der folgenden Bedingungen erfüllt ist: (1) Sie schreiben in alle Zellen der Spalte, oder (2) Es gibt bereits mindestens zwei unterschiedliche Formeln in der Spalte.</span><span class="sxs-lookup"><span data-stu-id="c9f63-795">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: callback
          description: '<span data-ttu-id="c9f63-796">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-796">Optional.</span></span> <span data-ttu-id="c9f63-797">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-797">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-798">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-798"></span></span> <span data-ttu-id="c9f63-799">Die AsyncResult. Value-Eigenschaft gibt immer undefined zurück, da kein Objekt oder Daten zum Abrufen vorhanden sind.</span><span class="sxs-lookup"><span data-stu-id="c9f63-799">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync
    summary: |-
      <span data-ttu-id="c9f63-800">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-800">Project documents only.</span></span> <span data-ttu-id="c9f63-801">Legen Sie das Vorgangsfeld für die angegebene Vorgangs-ID fest.</span><span class="sxs-lookup"><span data-stu-id="c9f63-801">Set task field for specified task Id.</span></span>

      <span data-ttu-id="c9f63-802">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-802">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-803">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-803">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-804">Vorgangsfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-804">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="c9f63-805">Wert des Ziel Felds.</span><span class="sxs-lookup"><span data-stu-id="c9f63-805">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="c9f63-806">Stellt eine Option zum Beibehalten von Kontextdaten eines beliebigen Typs, unverändert, zur Verwendung in einem Rückruf bereit.</span><span class="sxs-lookup"><span data-stu-id="c9f63-806">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="c9f63-807">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-807">Optional.</span></span> <span data-ttu-id="c9f63-808">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-808">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-809">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-809"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync_1
    summary: |-
      <span data-ttu-id="c9f63-810">Nur Projektdokumente.</span><span class="sxs-lookup"><span data-stu-id="c9f63-810">Project documents only.</span></span> <span data-ttu-id="c9f63-811">Legen Sie das Vorgangsfeld für die angegebene Vorgangs-ID fest.</span><span class="sxs-lookup"><span data-stu-id="c9f63-811">Set task field for specified task Id.</span></span>

      <span data-ttu-id="c9f63-812">**Wichtig**: Diese API funktioniert nur in Project 2016 auf dem Windows-Desktop.</span><span class="sxs-lookup"><span data-stu-id="c9f63-812">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-813">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-813">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="c9f63-814">Entweder eine Zeichenfolge oder ein Wert der Aufgaben-ID.</span><span class="sxs-lookup"><span data-stu-id="c9f63-814">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="c9f63-815">Vorgangsfelder.</span><span class="sxs-lookup"><span data-stu-id="c9f63-815">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="c9f63-816">Wert des Ziel Felds.</span><span class="sxs-lookup"><span data-stu-id="c9f63-816">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="c9f63-817">Optional.</span><span class="sxs-lookup"><span data-stu-id="c9f63-817">Optional.</span></span> <span data-ttu-id="c9f63-818">Eine Funktion, die aufgerufen wird, wenn der Rückruf zurückgegeben wird, deren einziger Parameter vom Typ [Office. AsyncResult](xref:office.Office.AsyncResult) ist</span><span class="sxs-lookup"><span data-stu-id="c9f63-818">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="c9f63-819">.</span><span class="sxs-lookup"><span data-stu-id="c9f63-819"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.bindings
    summary: '<span data-ttu-id="c9f63-820">Ruft ein Objekt ab, das Zugriff auf die im Dokument definierten Bindungen bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-820">Gets an object that provides access to the bindings defined in the document.</span></span>'
    remarks: |-
      <span data-ttu-id="c9f63-821">Das Document-Objekt wird nicht direkt in Ihrem Skript instanziiert.</span><span class="sxs-lookup"><span data-stu-id="c9f63-821">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="c9f63-822">Verwenden Sie zum Aufrufen von Elementen des Document-Objekts, die mit dem aktuellen Dokument oder Arbeitsblatt interagieren sollen, in Ihrem Skript `Office.context.document`.</span><span class="sxs-lookup"><span data-stu-id="c9f63-822">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="c9f63-823">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-823">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - office.Office.Bindings
  - uid: office.Office.Document.customXmlParts
    summary: '<span data-ttu-id="c9f63-824">Ruft ein Objekt ab, das die benutzerdefinierten XML-Komponenten im Dokument darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-824">Gets an object that represents the custom XML parts in the document.</span></span>'
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - office.Office.CustomXmlParts
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-825">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-825">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.Document.mode
    summary: '<span data-ttu-id="c9f63-826">Ruft den Modus ab, in dem sich das Dokument befindet.</span><span class="sxs-lookup"><span data-stu-id="c9f63-826">Gets the mode the document is in.</span></span>'
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - office.Office.DocumentMode
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-827">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-827">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: office.Office.Document.settings
    summary: '<span data-ttu-id="c9f63-828">Ruft ein Objekt ab, das die gespeicherten benutzerdefinierten Einstellungen des Inhalts- oder Aufgabenbereichs-Add-Ins für das aktuelle Dokument darstellt.</span><span class="sxs-lookup"><span data-stu-id="c9f63-828">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>'
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - office.Office.Settings
  - uid: office.Office.Document.url
    summary: '<span data-ttu-id="c9f63-829">Ruft die URL des Dokuments ab, das zurzeit in der Hostanwendung geöffnet ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-829">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="c9f63-830">Gibt null zurück, wenn die URL nicht verfügbar ist.</span><span class="sxs-lookup"><span data-stu-id="c9f63-830">Returns null if the URL is unavailable.</span></span>'
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="c9f63-831">Beispiele</span><span class="sxs-lookup"><span data-stu-id="c9f63-831">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```