### YamlMime:UniversalReference
items:
  - uid: office.Office.BindingSelectionChangedEventArgs
    summary: '<span data-ttu-id="68b12-101">Stellt Informationen zu der Bindung bereit, die das SelectionChanged-Ereignis ausgelöst hat.</span><span class="sxs-lookup"><span data-stu-id="68b12-101">Provides information about the binding that raised the SelectionChanged event.</span></span>'
    name: Office.BindingSelectionChangedEventArgs
    fullName: Office.BindingSelectionChangedEventArgs
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.BindingSelectionChangedEventArgs.binding
      - office.Office.BindingSelectionChangedEventArgs.columnCount
      - office.Office.BindingSelectionChangedEventArgs.rowCount
      - office.Office.BindingSelectionChangedEventArgs.startColumn
      - office.Office.BindingSelectionChangedEventArgs.startRow
      - office.Office.BindingSelectionChangedEventArgs.type
  - uid: office.Office.BindingSelectionChangedEventArgs.binding
    summary: '<span data-ttu-id="68b12-102">Ruft ein [Office. Binding-](xref:office.Office.Binding) Objekt ab, das die Bindung darstellt, die das SelectionChanged-Ereignis ausgelöst hat.</span><span class="sxs-lookup"><span data-stu-id="68b12-102">Gets an [Office.Binding](xref:office.Office.Binding) object that represents the binding that raised the SelectionChanged event.</span></span>'
    name: binding
    fullName: binding
    langs:
      - typeScript
    type: property
    syntax:
      content: 'binding: Binding;'
      return:
        type:
          - Binding
  - uid: office.Office.BindingSelectionChangedEventArgs.columnCount
    summary: "<span data-ttu-id=\"68b12-103\">Ruft die Anzahl der ausgewählten Spalten ab.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-103\">Gets the number of columns selected.</span></span> <span data-ttu-id=\"68b12-104\">Wenn eine einzelne Zelle ausgewählt wird, wird\_1 zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-104\">If a single cell is selected returns 1.</span></span>\n\n<span data-ttu-id=\"68b12-105\">Wenn Benutzer eine nicht zusammenhängende Auswahl treffen, wird die Anzahl der letzten zusammenhängenden Auswahl innerhalb der Bindung zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-105\">If the user makes a non-contiguous selection, the count for the last contiguous selection within the binding is returned.</span></span>\n\n<span data-ttu-id=\"68b12-106\">Für Word funktioniert diese Eigenschaft nur für Bindungen von [Office. BindingType](xref:office.Office.BindingType) \"Table\".</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-106\">For Word, this property will work only for bindings of [Office.BindingType](xref:office.Office.BindingType) \"table\".</span></span> <span data-ttu-id=\"68b12-107\">Lautet der Typ der Bindung \"matrix\", wird null zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-107\">If the binding is of type \"matrix\", null is returned.</span></span> <span data-ttu-id=\"68b12-108\">Für den Aufruf tritt auch ein Fehler auf, wenn die Tabelle zusammengeführte Zellen enthält, da die Struktur der Tabelle einheitlich sein muss, damit diese Eigenschaft richtig funktioniert.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-108\">Also, the call will fail if the table contains merged cells, because the structure of the table must be uniform for this property to work correctly.</span></span>"
    name: columnCount
    fullName: columnCount
    langs:
      - typeScript
    type: property
    syntax:
      content: 'columnCount: number;'
      return:
        type:
          - number
        description: |-


          #### <a name="examples"></a><span data-ttu-id="68b12-109">Beispiele</span><span class="sxs-lookup"><span data-stu-id="68b12-109">Examples</span></span>

          ```javascript
          // The following example adds an event handler for the SelectionChanged event to the binding with an id of myTable.
          // When the user changes the selection, the handler displays the coordinates of the first cell in the selection,
          // and the number of row and columns selected.
          function addSelectionHandler() {
              Office.context.document.bindings.getByIdAsync("myTable", function (result) {
                  result.value.addHandlerAsync("bindingSelectionChanged", myHandler);
              });
          }

          // Display selection start coordinates and row/column count.
          function myHandler(bArgs) {
              write("Selection start row/col: " + bArgs.startRow + "," + bArgs.startColumn);
              write("Selection row count: " + bArgs.rowCount);
              write("Selection col count: " + bArgs.columnCount);
          }
          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.BindingSelectionChangedEventArgs.rowCount
    summary: "<span data-ttu-id=\"68b12-110\">Ruft die Anzahl der ausgewählten Zeilen ab.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-110\">Gets the number of rows selected.</span></span> <span data-ttu-id=\"68b12-111\">Wenn eine einzelne Zelle ausgewählt wird, wird\_1 zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-111\">If a single cell is selected returns 1.</span></span>\n\n<span data-ttu-id=\"68b12-112\">Wenn Benutzer eine nicht zusammenhängende Auswahl treffen, wird die Anzahl der letzten zusammenhängenden Auswahl innerhalb der Bindung zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-112\">If the user makes a non-contiguous selection, the count for the last contiguous selection within the binding is returned.</span></span>\n\n<span data-ttu-id=\"68b12-113\">Für Word funktioniert diese Eigenschaft nur für Bindungen von [Office. BindingType](xref:office.Office.BindingType) \"Table\".</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-113\">For Word, this property will work only for bindings of [Office.BindingType](xref:office.Office.BindingType) \"table\".</span></span> <span data-ttu-id=\"68b12-114\">Lautet der Typ der Bindung \"matrix\", wird null zurückgegeben.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-114\">If the binding is of type \"matrix\", null is returned.</span></span> <span data-ttu-id=\"68b12-115\">Für den Aufruf tritt auch ein Fehler auf, wenn die Tabelle zusammengeführte Zellen enthält, da die Struktur der Tabelle einheitlich sein muss, damit diese Eigenschaft richtig funktioniert.</span><span class=\"sxs-lookup\"><span data-stu-id=\"68b12-115\">Also, the call will fail if the table contains merged cells, because the structure of the table must be uniform for this property to work correctly.</span></span>"
    name: rowCount
    fullName: rowCount
    langs:
      - typeScript
    type: property
    syntax:
      content: 'rowCount: number;'
      return:
        type:
          - number
        description: |-


          #### <a name="examples"></a><span data-ttu-id="68b12-116">Beispiele</span><span class="sxs-lookup"><span data-stu-id="68b12-116">Examples</span></span>

          ```javascript
          // The following example adds an event handler for the SelectionChanged event to the binding with an id of myTable.
          // When the user changes the selection, the handler displays the coordinates of the first cell in the selection,
          // and the number of row and columns selected.
          function addSelectionHandler() {
              Office.context.document.bindings.getByIdAsync("myTable", function (result) {
                  result.value.addHandlerAsync("bindingSelectionChanged", myHandler);
              });
          }

          // Display selection start coordinates and row/column count.
          function myHandler(bArgs) {
              write("Selection start row/col: " + bArgs.startRow + "," + bArgs.startColumn);
              write("Selection row count: " + bArgs.rowCount);
              write("Selection col count: " + bArgs.columnCount);
          }
          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.BindingSelectionChangedEventArgs.startColumn
    summary: |-
      <span data-ttu-id="68b12-117">Der nullbasierte Index der ersten Spalte der Auswahl ab der äußersten linken Spalte der Bindung.</span><span class="sxs-lookup"><span data-stu-id="68b12-117">The zero-based index of the first column of the selection counting from the leftmost column in the binding.</span></span>

      <span data-ttu-id="68b12-118">Wenn Benutzer eine nicht zusammenhängende Auswahl treffen, werden die Koordinaten der letzten zusammenhängenden Auswahl innerhalb der Bindung zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="68b12-118">If the user makes a non-contiguous selection, the coordinates for the last contiguous selection within the binding are returned.</span></span>

      <span data-ttu-id="68b12-119">Für Word funktioniert diese Eigenschaft nur für Bindungen von [Office. BindingType](xref:office.Office.BindingType) "Table".</span><span class="sxs-lookup"><span data-stu-id="68b12-119">For Word, this property will work only for bindings of [Office.BindingType](xref:office.Office.BindingType) "table".</span></span> <span data-ttu-id="68b12-120">Lautet der Typ der Bindung "matrix", wird null zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="68b12-120">If the binding is of type "matrix", null is returned.</span></span> <span data-ttu-id="68b12-121">Für den Aufruf tritt auch ein Fehler auf, wenn die Tabelle zusammengeführte Zellen enthält, da die Struktur der Tabelle einheitlich sein muss, damit diese Eigenschaft richtig funktioniert.</span><span class="sxs-lookup"><span data-stu-id="68b12-121">Also, the call will fail if the table contains merged cells, because the structure of the table must be uniform for this property to work correctly.</span></span>
    name: startColumn
    fullName: startColumn
    langs:
      - typeScript
    type: property
    syntax:
      content: 'startColumn: number;'
      return:
        type:
          - number
        description: |-


          #### <a name="examples"></a><span data-ttu-id="68b12-122">Beispiele</span><span class="sxs-lookup"><span data-stu-id="68b12-122">Examples</span></span>

          ```javascript
          // The following example adds an event handler for the SelectionChanged event to the binding with an id of myTable.
          // When the user changes the selection, the handler displays the coordinates of the first cell in the selection,
          // and the number of row and columns selected.
          function addSelectionHandler() {
              Office.context.document.bindings.getByIdAsync("myTable", function (result) {
                  result.value.addHandlerAsync("bindingSelectionChanged", myHandler);
              });
          }

          // Display selection start coordinates and row/column count.
          function myHandler(bArgs) {
              write("Selection start row/col: " + bArgs.startRow + "," + bArgs.startColumn);
              write("Selection row count: " + bArgs.rowCount);
              write("Selection col count: " + bArgs.columnCount);
          }
          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.BindingSelectionChangedEventArgs.startRow
    summary: |-
      <span data-ttu-id="68b12-123">Der nullbasierte Index der ersten Zeile der Auswahl ab der ersten Zeile der Bindung.</span><span class="sxs-lookup"><span data-stu-id="68b12-123">The zero-based index of the first row of the selection counting from the first row in the binding.</span></span>

      <span data-ttu-id="68b12-124">Wenn Benutzer eine nicht zusammenhängende Auswahl treffen, werden die Koordinaten der letzten zusammenhängenden Auswahl innerhalb der Bindung zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="68b12-124">If the user makes a non-contiguous selection, the coordinates for the last contiguous selection within the binding are returned.</span></span>

      <span data-ttu-id="68b12-125">Für Word funktioniert diese Eigenschaft nur für Bindungen von [Office. BindingType](xref:office.Office.BindingType) "Table".</span><span class="sxs-lookup"><span data-stu-id="68b12-125">For Word, this property will work only for bindings of [Office.BindingType](xref:office.Office.BindingType) "table".</span></span> <span data-ttu-id="68b12-126">Lautet der Typ der Bindung "matrix", wird null zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="68b12-126">If the binding is of type "matrix", null is returned.</span></span> <span data-ttu-id="68b12-127">Für den Aufruf tritt auch ein Fehler auf, wenn die Tabelle zusammengeführte Zellen enthält, da die Struktur der Tabelle einheitlich sein muss, damit diese Eigenschaft richtig funktioniert.</span><span class="sxs-lookup"><span data-stu-id="68b12-127">Also, the call will fail if the table contains merged cells, because the structure of the table must be uniform for this property to work correctly.</span></span>
    name: startRow
    fullName: startRow
    langs:
      - typeScript
    type: property
    syntax:
      content: 'startRow: number;'
      return:
        type:
          - number
        description: |-


          #### <a name="examples"></a><span data-ttu-id="68b12-128">Beispiele</span><span class="sxs-lookup"><span data-stu-id="68b12-128">Examples</span></span>

          ```javascript
          // The following example adds an event handler for the SelectionChanged event to the binding with an id of myTable.
          // When the user changes the selection, the handler displays the coordinates of the first cell in the selection,
          // and the number of row and columns selected.
          function addSelectionHandler() {
              Office.context.document.bindings.getByIdAsync("myTable", function (result) {
                  result.value.addHandlerAsync("bindingSelectionChanged", myHandler);
              });
          }

          // Display selection start coordinates and row/column count.
          function myHandler(bArgs) {
              write("Selection start row/col: " + bArgs.startRow + "," + bArgs.startColumn);
              write("Selection row count: " + bArgs.rowCount);
              write("Selection col count: " + bArgs.columnCount);
          }
          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.BindingSelectionChangedEventArgs.type
    summary: '<span data-ttu-id="68b12-129">Ruft einen [Office. EventType](xref:office.Office.EventType) -Enumerationswert ab, der die Art des ausgelösten Ereignisses identifiziert.</span><span class="sxs-lookup"><span data-stu-id="68b12-129">Gets an [Office.EventType](xref:office.Office.EventType) enumeration value that identifies the kind of event that was raised.</span></span>'
    name: type
    fullName: type
    langs:
      - typeScript
    type: property
    syntax:
      content: 'type: EventType;'
      return:
        type:
          - EventType